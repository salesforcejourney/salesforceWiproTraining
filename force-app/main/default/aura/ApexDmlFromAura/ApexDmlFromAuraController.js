({
  initHandler: function (component, event, helper) {
    helper.initHandlerHelper(component, event, helper);
  },

  createHandler: function (component, event, helper) {
    helper.createHandlerHelper(component, event, helper);
  }
});
