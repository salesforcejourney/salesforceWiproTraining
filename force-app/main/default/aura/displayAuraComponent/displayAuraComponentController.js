({
  initHandler: function (component, event, helper) {
    helper.initHandlerHelper(component, event);
  },

  handleSendAccountName: function (component, event, helper) {
    helper.handleSendAccountNameHelper(component, event);
  },

  handleSearchIndustryEvent: function (component, event, helper) {
    helper.handleSearchIndustryEventHelper(component, event);
  }
});
